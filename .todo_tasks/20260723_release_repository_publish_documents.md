# 发布执行报告

## 变更范围

- 新增 `docs/数仓基础知识/` 下 18 份数仓基础知识文档。
- 项目版本从 `1.0.0` 递增至 `1.0.1`。
- 同步 `docs/dbt-数仓教学/01-dbt-概念与项目结构.md` 中的项目版本示例。

## 验证记录

- `git diff --cached --check`：新增文档提交前通过。
- `ruby` YAML 解析与版本断言：`dbt_project.yml` 的项目名为 `dbt_demo`，版本为
  `1.0.1`。
- `git diff --check`：版本及教学文档变更无空白错误。
- 本机未安装 `dbt`，未执行 `dbt parse`。

## 发布结果

- 文档提交：`1d23cc4`（`docs: 新增数仓基础知识文档`）。
- 版本提交：`584b362`（`chore: 递增项目版本至 1.0.1`）。
- 推送分支：`origin/main`，已由 `7d13658` 更新至 `584b362`。
